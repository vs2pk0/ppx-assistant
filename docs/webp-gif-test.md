# 动画 WebP 导出 GIF 验证

2026-09-14：保存动画 WebP 时逐帧解码并导出 GIF；普通 GIF 继续保留原文件。

- 真机样本：相册已有 `PPX_1789347257769.webp`，2,589,820 字节。
- 真机运行转换器成功，Android ImageDecoder 识别输出为 AnimatedImageDrawable。
- Pillow 独立核对：输入与输出均为 277 帧、284×360、总时长 27,700ms。
- 输出 GIF：7,029,273 字节。逐帧 RGB 平均绝对误差约 13/255，来自固定 256 色量化。
- 单元测试、Lint Release、debug / release 构建通过；正式 APK 已用于真机安装。

透明区域使用白底；GIF 色彩与体积受格式限制。转换设有 400 万像素、1000 帧上限，失败不会将不完整文件写入相册。此验证直接使用手机实际文件调用转换器，没有将帖子分享链接的网络下载、图库按钮和相册 UI 播放算作端到端验证。
