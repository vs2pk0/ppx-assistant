# 助手图标设计记录

- 方式：内置 imagegen（未使用 CLI 或其他模型）。
- 参考：当前手机皮皮虾 6.2.0 APK 的图标，提取到 `pipixia-6.2.0-icon.webp`。
- 原图：`helper-icon.png`。构建资源以 sips 等比缩放到 512 px，未另行绘制或改变图像内容。
- 风格：粉色背景、黑色粗轮廓、白色小虾、金色星光，保留与宿主的视觉关联并区分助手身份。

## 最终生成提示词

Create one finished Android launcher icon for an independent Pipixia helper app, inspired by the attached current Pipixia icon. Keep the cheerful pink/coral background and bold near-black outline, white friendly abstract shrimp face with two expressive circular eyes, but redraw as a clearly distinct companion mascot: small rounded shrimp body, playful wink, tiny golden four-point sparkle near the upper right to suggest clean media and helpful tools. Minimal flat vector-like illustration, premium precise smooth outlines, strong legibility at 48px. Square full-bleed opaque warm coral-pink background, no rounded outer corners (Android applies its own mask), mascot entirely inside central 65% safe area. No text, letters, watermark, gradients, shadows, mockup, or border. Single centered finished 1024x1024 icon asset, not presentation sheet. Save generated image to the project workspace docs/design/ if supported.
